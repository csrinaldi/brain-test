{
  "_provenance": {
    "endpoint": "GET /projects/:id/merge_requests?state=opened&per_page=50",
    "date": "2026-07-29",
    "derived": true,
    "note": "No live GitLab mirror is reachable from this environment (record-fixtures.mjs:27-34's standing constraint) — hand-authored from the documented GitLab REST API v4 merge_requests list shape (https://docs.gitlab.com/ee/api/merge_requests.html), trimmed to the fields gitlab.mjs#mrList actually consumes (iid, title, source_branch, state — the last additive per #930, native GitLab enum). Two entries, matching the github-mrList-happy.json discipline of exercising the per-entry assertion loop genuinely, both `opened` to match the `state=opened` query."
  },
  "data": [
    {
      "iid": 42,
      "title": "Add pagination guard to labelList",
      "source_branch": "feat/label-pagination",
      "state": "opened"
    },
    {
      "iid": 41,
      "title": "Fix issueView author normalization",
      "source_branch": "fix/issue-author-null",
      "state": "opened"
    }
  ]
}
